name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2

      - name: Validate plugin structure
        run: bun scripts/validate-plugin-structure.js

      - name: Validate config (CI mode)
        run: bun scripts/validate-config.js --ci

      - name: Validate STRUCTURE.md
        run: bun scripts/validate-structure.js

      - name: Test PII guard
        run: bun scripts/test-pii-guard.js

      - name: Validate markdown links
        run: bun scripts/validate-links.js

      - name: Run tests
        run: bun test
